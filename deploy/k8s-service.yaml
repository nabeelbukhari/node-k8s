apiVersion: v1
kind: Service
metadata:
  name: node-k8s-loadtest-service
spec:
  type: LoadBalancer
  selector:
    app: node-k8s-loadtest
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
